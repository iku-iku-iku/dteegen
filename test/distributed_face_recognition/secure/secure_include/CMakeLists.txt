
if(${CMAKE_SYSTEM_PROCESSOR} MATCHES "x86_64")
  set(INCLUDE_DIRS
  "${CMAKE_CURRENT_SOURCE_DIR}/ncnn.rv"
)
elseif(${CMAKE_SYSTEM_PROCESSOR} MATCHES "riscv")
  set(INCLUDE_DIRS
  "${CMAKE_CURRENT_SOURCE_DIR}/ncnn.x64"
)
endif()

set(INCLUDE_DIRS ${INCLUDE_DIRS} PARENT_SCOPE)
